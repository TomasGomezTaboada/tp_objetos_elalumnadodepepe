import LaTierraMedia.Baculo

describe "Pruebas de báculo" {

  test "un báculo para un guerrero con mucha vida" {
    var baculoBase = new Baculo()
    assert.equals(250, baculoBase.poderOtorgado())
  }

}